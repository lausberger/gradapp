# frozen_string_literal: true

Given(/^I am on the approve applications page$/) do
  pending
end

And(/^There are the following applications created:$/) do |_table|
  # table is a table.hashes.keys # => [:first_name, :last_name, :email, :phone, :dob, :status]
  pending
end

And(/^I see the following applications:$/) do |_table|
  # table is a table.hashes.keys # => [:first_name, :last_name, :email, :phone, :dob, :status]
  pending
end

When(/^I "([^"]*)" the following application:$/) do |_arg, _table|
  # table is a table.hashes.keys # => [:first_name, :last_name, :email, :phone, :dob, :status]
  pending
end

Then(/^I should no longer see the following application:$/) do |_table|
  # table is a table.hashes.keys # => [:first_name, :last_name, :email, :phone, :dob, :status]
  pending
end
