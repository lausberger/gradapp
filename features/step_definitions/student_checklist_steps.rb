Given(/^The following account is created:$/) do |table|
  # table is a table.hashes.keys # => [:first_name, :last_name, :email, :password, :type]
  pending
end

And(/^The an empty Student Checklist is created for student with email "([^"]*)"$/) do |arg|
  pending
end

And(/^I am on the student checklist page$/) do
  pending
end

And(/^The following Student Checklist for student with email "([^"]*)":$/) do |arg, table|
  # table is a table.hashes.keys # => [:citizenship, :research_area, :degree_objective]
  pending
end

Then(/^I should see an empty student checklist$/) do
  pending
end

Then(/^I should see the following items have been completed:$/) do |table|
  # table is a table.hashes.keys # => [:type]
  pending
end
