Given(/^I am on the approve faculty accounts page$/) do
  pending
end

And(/^There are the following accounts created:$/) do |table|
  # table is a table.hashes.keys # => [:first_name, :last_name, :email, :password, :account_type, :topic_area]
  pending
end

When(/^I approve the following faculty account:$/) do |table|
  # table is a table.hashes.keys # => [:first_name, :last_name, :email, :password, :account_type, :topic_area]
  pending
end

Then(/^I should see the following faculty accounts:$/) do |table|
  # table is a table.hashes.keys # => [:first_name, :last_name, :email, :password, :account_type, :topic_area]
  pending
end

Then(/^I should no long see the following account:$/) do |table|
  # table is a table.hashes.keys # => [:first_name, :last_name, :email, :password, :account_type, :topic_area]
  pending
end
