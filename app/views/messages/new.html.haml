%h1.title New Message
= form_tag 'send_message', :method => :post, :id => 'send_message' do
  = label :to, 'To'
  %div
  = text_field :to, params[:to], maxlength: 40, size: 31, :id => 'to_box'
  %div
  = label :subject, 'Subject'
  %div
  = text_field :subject, params[:subject], maxlength: 40, size: 31, :id => 'subject_box'
  %div
  = label :body, 'Body'
  %div
  = text_area :body, params[:body], cols: 40, rows: 10, :id => 'body_box'

  #spacer
    = submit_tag "Send Message", :id => 'send_button'
  = link_to "Go Back", messages_path

