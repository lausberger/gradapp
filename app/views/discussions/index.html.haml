%h1 Student Discussion Posts

%table
  %thead
    %th Title
    %th Body
    %th Author
    %th View Replies
  %tbody
    - @discussions.each do | discussion |
      %tr
        %td= discussion[:title]
        %td= discussion[:body]
        %td= "#{discussion.account[:first_name]} #{discussion.account[:last_name]}"
        %td= link_to "View Replies", discussion_path(discussion)
        %td
        - if !@current_user.nil? and @current_user[:first_name] == discussion.account[:first_name] and @current_user[:last_name] == discussion.account[:last_name]
          %td= link_to "Edit", edit_discussion_path(discussion)
          %td
          %td= link_to "Delete", discussion_path(discussion), method: :delete

%br

- if !@current_user.nil?
  =link_to "Post new Discussion", new_discussion_path, {:id => "post_new_discussion"}
