%h1= "The following faculties members focus in #{@topic_area}"

%table
  %thead
    %th First Name
    %th Last Name
  %tbody
    - @faculties.each do |faculty|
      %tr
        %td= faculty.account[:first_name]
        %td= faculty.account[:last_name]

= link_to 'View All Faculty Members', faculties_path
